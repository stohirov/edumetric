package com.edumetric.backend.reminders.domain;

/** What a {@link ReminderLog} row records having already reminded a user about. */
public enum ReminderType {
    UPCOMING_LESSON,
    ASSIGNMENT_DUE
}
